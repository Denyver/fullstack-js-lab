// Estrutura de repetição

for (let i = 0; i <= 5; i++) {
    const numPar = i % 2 ? 'Impar' : 'Par';
    console.log(i, numPar)
}

const personagens = ['zory', 'nix', 'haghii'];

for (let i = 0; i < personagens.length; i++) {
    console.log(personagens[i])
}